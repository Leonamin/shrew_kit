import 'package:flutter/material.dart';

class TestSliderView extends StatelessWidget {
  const TestSliderView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
